{
    "name": "allo-capital-allocator",
    "version": "1.0.0",
    "description": "Quadratic Governance & AI-Treasury for Allo Protocol",
    "scripts": {
        "test": "hardhat test",
        "deploy": "hardhat run scripts/deploy.js --network sepolia"
    },
    "dependencies": {
        "@nomicfoundation/hardhat-toolbox": "^3.0.0",
        "dotenv": "^16.0.3",
        "hardhat": "^3.0.0"
    }
}